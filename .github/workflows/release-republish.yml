# Manual repair of a release image — retag (registry-side copy, preferred) or rebuild.
#
# THIS FILE IS RENDERED, and it had better be. It carried no Jinja at all and was
# byte-identical across every service render, which meant it hardcoded
# `ghcr.io/${{ github.repository }}` and passed no `context:` — so the one manual
# repair path was wrong for exactly the repos most likely to need it: the ones
# whose image or build context is not the default. It was found during an
# incident, which is the worst possible moment to discover that the recovery tool
# is pointed at the wrong repository.
#
# NO APP SECRETS TO ci-build / ci-smoke. It used to pass APP_ID and
# APP_PRIVATE_KEY to both, which the README warns in bold against and ci.yaml has
# never done: ci-build logs in with the App token whenever APP_ID is non-empty,
# and a typical App installation has no package-write grant, so passing it there
# does not merely do nothing — it FAILS with `denied: permission_denied`. The
# README rule was right and this file was wrong. GITHUB_TOKEN reaches every
# package under the repo's own owner that this repo created, which is what both
# the ordinary pipeline and this repair path push to.
#
# REPAIRS THE MANAGER ONLY — a known limit, not an oversight. This repo publishes
# two images, but both the retag and rebuild paths here take a single hardcoded
# IMAGE, and `task smoke` only knows how to assert the manager binary. Extending
# it would mean threading an image choice through all three jobs plus teaching
# smoke a second shape, which is real work with no incident asking for it yet.
# To repair the agent by hand:
#   docker buildx imagetools create \
#     -t ghcr.io/jacaudi/wireguard-operator/agent:vX.Y.Z \
#     ghcr.io/jacaudi/wireguard-operator/agent:sha-<short>
name: release-republish

on:
  workflow_dispatch:
    inputs:
      tag:
        description: Existing release tag to repair (e.g. v1.2.3)
        required: true
        type: string
      mode:
        description: retag the CI-built image (preferred), or rebuild from the tag
        required: true
        default: retag
        type: choice
        options:
          - retag
          - rebuild

concurrency:
  group: release-republish-${{ inputs.tag }}
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  retag:
    name: retag ${{ inputs.tag }}
    if: ${{ inputs.mode == 'retag' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    outputs:
      image: ${{ steps.out.outputs.image }}
    env:
      APP_ID: ${{ secrets.APP_ID }}
      # The SAME name ci.yaml gives ci-build and ci-retag. Hardcoded, this job
      # repaired a repository the pipeline never publishes to.
      IMAGE: ghcr.io/jacaudi/wireguard-operator/manager
    steps:
      - name: Resolve the tag to its commit
        id: commit
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ inputs.tag }}
        run: |
          set -euo pipefail
          sha=$(gh api "repos/${GITHUB_REPOSITORY}/git/refs/tags/${TAG}" --jq '.object.sha')
          type=$(gh api "repos/${GITHUB_REPOSITORY}/git/refs/tags/${TAG}" --jq '.object.type')
          if [ "${type}" = "tag" ]; then
            sha=$(gh api "repos/${GITHUB_REPOSITORY}/git/tags/${sha}" --jq '.object.sha')
          fi
          echo "sha=${sha}" >> "$GITHUB_OUTPUT"
          echo "short=sha-${sha:0:7}" >> "$GITHUB_OUTPUT"
          echo "${TAG} -> ${sha} (${sha:0:7})"

      - name: Generate a GitHub App token
        id: app-token
        if: ${{ env.APP_ID != '' }}
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          app-id: ${{ secrets.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}

      - name: Log in to GHCR
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          registry: ghcr.io
          username: ${{ steps.app-token.outputs.token && 'x-access-token' || github.actor }}
          password: ${{ steps.app-token.outputs.token || secrets.GITHUB_TOKEN }}

      - name: Verify the source image was built from the tagged commit
        env:
          SRC: ${{ env.IMAGE }}:${{ steps.commit.outputs.short }}
          WANT: ${{ steps.commit.outputs.sha }}
          TAG: ${{ inputs.tag }}
        run: |
          set -euo pipefail
          echo "source image: ${SRC}"
          docker pull -q "${SRC}"
          have=$(docker inspect --format '{{index .Config.Labels "org.opencontainers.image.revision"}}' "${SRC}")
          echo "tag commit:     ${WANT}"
          echo "image revision: ${have}"
          [ "${WANT}" = "${have}" ] || { echo "::error::${SRC} was built from ${have}, refusing to retag it as ${TAG}"; exit 1; }

      - name: Point the semver tags at that image
        id: out
        env:
          SRC: ${{ env.IMAGE }}:${{ steps.commit.outputs.short }}
          TAG: ${{ inputs.tag }}
        run: |
          set -euo pipefail
          ver="${TAG#v}"
          # Refuse prereleases: metadata-action never produced :v1 / :v1.2 for
          # them, so moving those tags here would clobber the last stable release.
          case "${ver}" in
            *-*|*+*) echo "::error::${TAG} is a prerelease; ci-build publishes no moving tags for it"; exit 1 ;;
          esac
          case "${ver}" in
            [0-9]*.[0-9]*.[0-9]*) ;;
            *) echo "::error::${TAG} is not vMAJOR.MINOR.PATCH"; exit 1 ;;
          esac
          major="${ver%%.*}"
          minor="${ver%.*}"
          docker buildx imagetools create \
            -t "${IMAGE}:v${ver}" \
            -t "${IMAGE}:v${minor}" \
            -t "${IMAGE}:v${major}" \
            "${SRC}"
          digest=$(docker buildx imagetools inspect "${IMAGE}:v${ver}" --format '{{json .Manifest.Digest}}' | tr -d '"')
          echo "image=${IMAGE}@${digest}" >> "$GITHUB_OUTPUT"
          echo "v${ver}, v${minor}, v${major} -> ${digest}"

  rebuild:
    name: rebuild ${{ inputs.tag }}
    if: ${{ inputs.mode == 'rebuild' }}
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/ci-build.yml
    with:
      version: ${{ inputs.tag }}
      ref: ${{ inputs.tag }}
      image: ghcr.io/jacaudi/wireguard-operator/manager
      # REQUIRED HERE TOO. Neither image sits at its build context root, so
      # without this the rebuild path fails with "no Dockerfile or Containerfile
      # in ." — and it would fail during an incident, which is the worst moment
      # to find out the repair tool never worked. See ci-build.yml's `file:` input.
      file: images/manager/Dockerfile

  smoke:
    needs: [retag, rebuild]
    if: ${{ !cancelled() && (needs.retag.result == 'success' || needs.rebuild.result == 'success') }}
    permissions:
      contents: read
      packages: read
    uses: ./.github/workflows/ci-smoke.yml
    with:
      image: ${{ needs.retag.outputs.image || needs.rebuild.outputs.image }}
